{
  "name": "dev-astra",
  "version": "0.2.2",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter @dev-astra/web dev",
    "build": "bun run --filter @dev-astra/web build",
    "test": "bun run --filter @dev-astra/core test",
    "preview": "bun run --filter @dev-astra/web preview"
  }
}
